<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Task Manager</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(120deg, #f1f2f6, #dbe9f4);
            min-height: 100vh;
        }
        .hero-card {
            border-radius: 20px;
            padding: 2rem;
            background: #ffffff;
            box-shadow: 0 5px 25px rgba(0, 0, 0, 0.1);
        }
        .btn-lg {
            border-radius: 12px;
        }
    </style>
</head>
<body>
    <div class="container d-flex flex-column justify-content-center align-items-center" style="min-height: 100vh;">
        <div class="hero-card text-center w-100" style="max-width: 500px;">
            <h1 class="mb-4 fw-bold">Welcome to Task Manager</h1>

            <% if (!user) { %>
                <p class="mb-4 text-muted">Organize your tasks efficiently. Login or register to get started.</p>
                <a href="/login" class="btn btn-primary btn-lg w-100 mb-3">Login</a>
                <a href="/register" class="btn btn-outline-secondary btn-lg w-100">Register</a>
            <% } else { %>
                <p class="mb-4 text-muted">Welcome back, <strong><%= user.username %></strong>!</p>
                <a href="/tasks" class="btn btn-success btn-lg w-100 mb-3">Go to My Tasks</a>

                <% if (user.role === 'admin') { %>
                    <a href="/users" class="btn btn-warning btn-lg w-100 mb-3">Manage Users</a>
                <% } %>

                <a href="/logout" class="btn btn-outline-danger btn-lg w-100">Logout</a>
            <% } %>
        </div>
    </div>
</body>
</html>
